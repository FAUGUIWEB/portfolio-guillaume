name: Rebuild New Drop Effect
on:
  push:
    paths:
      - .github/workflows/rebuild-new-drop.yml
permissions:
  contents: write
jobs:
  rebuild:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo ok
