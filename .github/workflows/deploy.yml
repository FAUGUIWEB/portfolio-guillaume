name: Deploy to OVH

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Apply ultra smooth New Drop
        if: ${{ hashFiles('ultra_smooth_new_drop.py') != '' }}
        shell: bash
        run: |
          python3 ultra_smooth_new_drop.py
          rm -f ultra_smooth_new_drop.py optimize_new_drop_soul.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Make New Drop soul animation ultra smooth"
          git push

      - name: Deploy to OVH
        uses: SamKirkland/FTP-Deploy-Action@v4.3.6
        with:
          server: ftp.cluster129.hosting.ovh.net
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftp
          port: 21
          server-dir: /www/
